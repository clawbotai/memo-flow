# Tasks

- [x] Task 1: Setup Linear Design Tokens
  - [x] Update `tailwind.config.ts` with Linear-style colors (Zinc/Slate palette), animations (fade-in, slide-up), and border radius.
  - [x] Update `src/styles/globals.css` with new CSS variables for background, foreground, border, and primary colors (focus on dark mode).
  - [x] Verify global background and text colors in a new terminal.

- [x] Task 2: Refine Core Components
  - [x] Update `Button` component (`src/components/ui/button.tsx`) with subtle borders, inner shadows, and hover effects.
  - [x] Update `Input` and `Textarea` components (`src/components/ui/input.tsx`) with refined focus rings and background styles.
  - [x] Update `Card` component (`src/components/ui/card.tsx`) with Linear-style borders and background.
  - [x] Update `Badge` and `Skeleton` components if necessary.

- [x] Task 3: Update Layout and Global UI
  - [x] Update `src/app/layout.tsx` to apply new background and text classes.
  - [x] Refine `Navbar` or `Header` (if applicable) to match the new style.
  - [x] Ensure `Toast` notifications (`src/components/ui/toast.tsx`) match the new aesthetic.

- [x] Task 4: Polish and Verification
  - [x] Check `Analysis` page (`src/app/analysis/[id]/page.tsx`) for visual consistency.
  - [x] Check `Note` page (`src/app/note/[id]/page.tsx`) for visual consistency.
  - [x] Verify responsiveness on mobile and desktop.
  - [x] Ensure no functional regressions (buttons clickable, inputs focusable).
